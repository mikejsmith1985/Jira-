{
  "name": "@jira-plus/core",
  "version": "0.1.0",
  "private": true,
  "description": "Every rule in Jira+, compiled once for both the browser and Node so no rule is ever ported twice",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  }
}
