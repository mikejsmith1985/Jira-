// setup.ts — Test environment for the client suites.
//
// jest-dom's matchers are registered here so every suite can assert on what a
// user would actually see, rather than on implementation details of the DOM.

import "@testing-library/jest-dom/vitest";
